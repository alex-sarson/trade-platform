// Phase 1: outstanding total, overdue list, upcoming jobs, and
// completed-but-not-yet-invoiced jobs — see brief §6 (dashboard/overview).
export function DashboardPage() {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Outstanding invoices, overdue alerts and upcoming jobs will live here.</p>
    </div>
  );
}
