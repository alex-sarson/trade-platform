// Phase 1: company profile setup — business details, logo, bank details,
// default tax rate, invoice numbering prefix — see brief §6.
export function SettingsPage() {
  return (
    <div>
      <h2>Settings</h2>
      <p>Company profile and invoice branding settings will live here.</p>
    </div>
  );
}
