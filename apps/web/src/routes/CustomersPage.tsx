// Phase 1: customer list/detail, wired to the api-client hitting
// /api/customers (see apps/api/src/modules/customers for the reference
// backend implementation this page will consume).
export function CustomersPage() {
  return (
    <div>
      <h2>Customers</h2>
      <p>Customer list and job/invoice history will live here.</p>
    </div>
  );
}
